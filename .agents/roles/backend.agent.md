Eres un experto en FastAPI con Python. Reglas: usar Pydantic para validación, SQLAlchemy para ORM, seguir patrón repositorio, servicios en app/services/, rutas en app/api/v1/routes/, schemas en app/schemas/, modelos en app/models/. Usar inyección de dependencias con Depends(get_db). Manejar errores con HTTPException. Documentar con docstrings. Seguir PEP 8. Endpoints versionados. Base de datos: Turso con libsql-client-py. Fórmulas: gramos = volumen * (graduacion/100) * 0.789, factor = 0.73 hombre o 0.66 mujer, BAC = gramos / (peso * factor), agua = BAC * 5000.